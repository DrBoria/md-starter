// This file is automatically generated by Keystone, do not modify it manually.
// Modify your Keystone config when you want to change this.

datasource postgresql {
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
  provider          = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Example {
  id                   String    @id @default(cuid())
  shortedText          String    @default("") @map("shorted_text")
  exampleType          String
  customRelationship   User?     @relation("Example_customRelationship", fields: [customRelationshipId], references: [id])
  customRelationshipId String?   @map("user_id")
  checkbox             Boolean   @default(true) @map("is_active")
  timestamp_updateAt   DateTime? @updatedAt @map("updated_at")
  timestamp_createdAt  DateTime  @default(now()) @map("created_at")

  @@index([customRelationshipId])
  @@map("example")
}

model User {
  id                              String    @id @default(cuid())
  email                           String    @unique @default("")
  password                        String?
  locked                          Boolean   @default(false)
  role                            Role?     @relation("User_role", fields: [roleId], references: [id])
  roleId                          String?   @map("role")
  createdAt                       DateTime  @default(now()) @map("created_at")
  from_Example_customRelationship Example[] @relation("Example_customRelationship")

  @@index([roleId])
  @@map("user")
}

model Role {
  id             String   @id @default(cuid())
  name           String   @unique @default("")
  createdAt      DateTime @default(now()) @map("created_at")
  from_User_role User[]   @relation("User_role")

  @@map("role")
}

model Post {
  id          String    @id @default(cuid())
  name        String    @unique @default("")
  premium     Boolean   @default(false)
  textContent String    @default("") @map("text_content")
  updateAt    DateTime? @updatedAt @map("updated_at")
  createdAt   DateTime  @default(now()) @map("created_at")

  @@map("post")
}
