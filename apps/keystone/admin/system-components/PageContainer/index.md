```jsx
import { PageContainer } from "admin/system-components/PageContainer";
import { KeystoneProvider } from "admin/utils/ui-kit/KeystoneProvider";

// KeystoneProvider is only for UI-KIT
// Don't use it in application
<KeystoneProvider>
  <PageContainer header="Styleguidist Demo">
    Here you can past content of your page
  </PageContainer>
</KeystoneProvider>;
```
