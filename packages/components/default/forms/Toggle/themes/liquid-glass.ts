import { css } from "styled-components";

export const liquidGlassTheme = css`
  /* LIQUID GLASS THEME: Toggle (Glass Accordion) */
  background: rgb(255 255 255 / 5%);
  border-radius: 8px;
  padding: 8px;
  margin-bottom: 8px;
  
  .toggle-header {
    font-weight: 600;
  }
`;
